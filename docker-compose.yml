services:
  opennews-collector:
    build:
      context: .
    image: trading-opennews-collector:latest
    env_file:
      - .env
    environment:
      OPENNEWS_DATA_DIR: /data/opennews
    command:
      - --profile
      - combined
      - --engine-type
      - news
      - --engine-type
      - onchain
      - --split-profile-by-engine
      - --adaptive-pages
      - --limit
      - "20"
      - --min-pages
      - "1"
      - --max-pages
      - "100"
      - --page-step
      - "2"
    volumes:
      - /root/trading/data/opennews:/data/opennews
    restart: "no"
